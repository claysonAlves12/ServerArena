{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@now/node" }
  ],
  "routes": [
    { "src": "/public/(.*)", "dest": "/src/public/$1" },
    { "src": "/views/(.*)", "dest": "/src/views/$1" },

  ]
}
